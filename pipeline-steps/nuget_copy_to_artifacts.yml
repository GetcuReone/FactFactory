steps:
- script: |
    echo Install file $(snk_file.secureFilePath)

    cp '$(snk_file.secureFilePath)' '$(Build.Repository.LocalPath)/FactFactory'
    echo copied '$(snk_file.secureFilePath)' to '$(Build.Repository.LocalPath)\FactFactory'
  displayName: 'Сopy file snk to projects'
